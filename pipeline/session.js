// [C] Stage 1 — a session with a body. PLAN.md §10.
// Steel session at 1440×900 with a saved profile (cookies + localStorage from
// a Google account logged in by hand, once, ahead of time) injected. The
// browser wakes up past the login wall. This is why auth isn't a problem.
import { VIEWPORT, STEEL_API_KEY, PROFILE_PATH } from './config.js';

export async function openSession() {
  // TODO [C]
}

export async function closeSession(session) {
  // TODO [C]
}
