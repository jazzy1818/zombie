// [C] Canned pipeline run for the live demo segment, in case the network,
// Steel, or the model is having a bad day at the worst possible moment.
// PLAN.md §14: keep the live task trivially shallow (2–3 steps, ~40s).

export async function fallbackDemo() {
  // TODO [C]
}
