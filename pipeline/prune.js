// [C] Stage 3 — pruning.
// Replay backwards from the success state; keep only load-bearing actions.
//
// If you ship the raw trace, the teacher walks users through the agent's
// mistakes. You'd be teaching confusion. The pruned path is the lesson.

export async function prune(session, trace) {
  // TODO [C]
}
