// [D] Correct/wrong pulses + scrollIntoView helper.
import { CORRECT, WRONG } from '../constants.js';

export function flashCorrect()   { /* TODO [D] — green pulse on current spotlight */ }
export function flashWrong(box)  { /* TODO [D] — red pulse at box, then fade */ }
